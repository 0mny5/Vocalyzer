$(document).on('ready', function () {
	// initialization of text animation (typing)
	$(".js-display-typing").typed({
		strings: [
			"Vocalyzer",
			"Creative",
			"Innovative"
		],
		typeSpeed: 60,
		loop: true,
		backDelay: 2500
	});
});
