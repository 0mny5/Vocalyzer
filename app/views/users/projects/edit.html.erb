<h1>Editing Project</h1>

<%= render 'form', project: @project %>

<%= link_to 'Show', users_project_path(@project) %>
<% if current_user.twitter? %>
  <%= link_to 'Back', users_projects_path %>
<% end %>
