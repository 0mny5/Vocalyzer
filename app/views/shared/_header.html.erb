<nav class="js-navbar-scroll navbar fixed-top navbar-expand-lg bg-dark">
  <div class="container">
    <%= link_to 'Vocalyzer', root_path, class: 'navbar-brand text-white' %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse offset-lg-8" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle text-white" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              <%= t('defaults.project_tasks') %>
            </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdown">
          <% if current_user.twitter? || current_user.projects.count.zero? %>
            <%= link_to t('users.projects.new.create'), new_users_project_path, class: 'dropdown-item' %>
          <% end %>
            <%= link_to t('users.projects.index.title'), users_projects_path, class: 'dropdown-item' %>
          </div>
        </li>
      </ul>
      <ul class="navbar-nav mr-auto">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle text-white" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <% if current_user.avatar.attached? %>
              <%= image_tag current_user.avatar, class: 'rounded-circle', size: '30x30' %>
            <% end %>
          </a>
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
            <h6 class="dropdown-header text-center"><%= "#{current_user.name}さん" %></h6>
            <div class="dropdown-divider"></div>
            <%= link_to t('users.profiles.show.title'), users_profile_path, class: 'dropdown-item text-center' %>
            <% if current_user.twitter? %>
              <%= link_to t('defaults.logout'), logout_path, class: 'dropdown-item text-center', method: :delete %>
            <% else %>
              <%= link_to t('defaults.logout'), logout_path, class: 'dropdown-item text-center', method: :delete %>
            <% end %>
          </div>
        </li>
      </ul>
    </div>
  </div>
</nav>
