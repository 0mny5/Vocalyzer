<nav class="js-navbar-scroll navbar fixed-top navbar-expand-lg bg-dark">
  <div class="container">
    <%= link_to 'VocalyzerAdmin', root_path, class: 'navbar-brand text-white' %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse offset-lg-9" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item dropdown">
          <% if current_admin_user %>
            <a class="nav-link dropdown-toggle text-white" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              <%= "#{current_admin_user.name}" %>
            </a>
          <% end %>
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
            <%= link_to t('defaults.logout'), admin_logout_path, class: 'dropdown-item text-center', method: :delete %>
          </div>
        </li>
      </ul>
    </div>
  </div>
</nav>
